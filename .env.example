# Rapid Word Flutter example environment values
#
# This project currently uses --dart-define at runtime/build time.
# You can keep this file as a reference and manually map values into your
# flutter run / flutter build commands.

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_publishable_key_here
